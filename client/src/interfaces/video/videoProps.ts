export interface video {
  _id: string;
  name: string;
  tempFilePath: string;
  description: string;
}

export interface videoProps {
  videos: video[];
}

export interface video {
  _id: string;
  name: string;
  tempFilePath: string;
  description: string;
}

export interface file {
  name: string;
}
